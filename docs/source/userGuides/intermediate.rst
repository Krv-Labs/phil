.. _intermediate:

============
Intermediate
============

Tune through gallery names and method grids.

- Use ``GridGallery.get("finance")`` for finance-oriented defaults.
- Increase sample count for better diversity at higher compute cost.
- Compare method grids by setting ``param_grid`` as a custom ``ImputationConfig``.
